PUBLIC_SANITY_PROJECT_ID=dubig5fl
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2026-06-15
SANITY_USE_CDN=false

# Keep secret values without the PUBLIC_ prefix.
# Only set this if you need to read private datasets or draft content at build time.
SANITY_API_TOKEN=
